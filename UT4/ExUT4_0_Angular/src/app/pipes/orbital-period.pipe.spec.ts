import { OrbitalPeriodPipe } from './orbital-period.pipe';

describe('OrbitalPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new OrbitalPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
