import { RotationPeriodPipe } from './rotation-period.pipe';

describe('RotationPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new RotationPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
